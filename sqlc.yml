version: "2"
sql:
    - schema: "sql/schema"
      queries: "sql/queries"
      engine: "postgresql"
      gen:
          go:
              package: "repository"
              out: "internal/repository"
              emit_interface: true
              emit_json_tags: true
